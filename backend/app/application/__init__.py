"""Application layer for the temporary backend.

The temporary decision-history service composes the pure policy layer with the
persistence layer. The policy layer itself remains independent of this module.
"""
